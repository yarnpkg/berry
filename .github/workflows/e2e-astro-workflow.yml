on:
  schedule:
  - cron: '0 */4 * * *'
  push:
    branches:
    - master
  pull_request:
    paths:
    - .github/workflows/e2e-astro-workflow.yml
    - scripts/e2e-setup-ci.sh

name: 'E2E Astro'
jobs:
  chore:
    name: 'Validating Astro'
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master

    - name: 'Install Node'
      uses: actions/setup-node@master
      with:
        node-version: 14.x

    - name: 'Build the standard bundle'
      run: |
        node ./scripts/run-yarn.js build:cli

    - name: 'Running the integration test'
      run: |
        source scripts/e2e-setup-ci.sh
        yes | yarn create astro my-app && cd my-app
        YARN_PNP_FALLBACK_MODE=all YARN_PNP_MODE=loose yarn
        yarn build
