export const pluginCommands = new Map([
  [`constraints`, [
    [`constraints`, `query`],
    [`constraints`, `source`],
    [`constraints`],
  ]],
  [`interactive-tools`, [
    [`upgrade-interactive`],
  ]],
  [`stage`, [
    [`stage`],
  ]],
  [`version`, [
    [`version`, `apply`],
    [`version`, `check`],
    [`version`],
  ]],
  [`workspace-tools`, [
    [`workspaces`, `foreach`],
    [`workspace`],
  ]],
]);
